spring:
  application:
    name: ${SPRING_APPLICATION_NAME:ibkr-dashboard}

  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:trading_db}
    username: ${POSTGRES_USER:trading_user}
    password: ${POSTGRES_PASSWORD:trading_pass}
    driver-class-name: ${POSTGRES_DRIVER:org.postgresql.Driver}
    hikari:
      maximum-pool-size: ${DB_POOL_MAX_SIZE:10}
      minimum-idle: ${DB_POOL_MIN_IDLE:2}
      connection-timeout: ${DB_CONNECTION_TIMEOUT_MS:30000}

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:validate}
    show-sql: ${JPA_SHOW_SQL:false}
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true

  flyway:
    enabled: ${FLYWAY_ENABLED:true}
    locations: ${FLYWAY_LOCATIONS:classpath:db/migration}
    baseline-on-migrate: ${FLYWAY_BASELINE_ON_MIGRATE:true}
    validate-on-migrate: ${FLYWAY_VALIDATE_ON_MIGRATE:true}

server:
  port: ${SERVER_PORT:8080}
  error:
    include-message: ${SERVER_ERROR_INCLUDE_MESSAGE:always}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: alpha
    tags-sorter: alpha

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: ${MANAGEMENT_HEALTH_SHOW_DETAILS:when-authorized}

ibkr:
  account-id: ${IBKR_ACCOUNT_ID:}
  tws:
    host: ${IBKR_TWS_HOST:127.0.0.1}
    port: ${IBKR_TWS_PORT:7497}
    client-id: ${IBKR_TWS_CLIENT_ID:101}
    timeout-seconds: ${IBKR_TWS_TIMEOUT_SECONDS:20}
  flex:
    token: ${IBKR_FLEX_TOKEN:}
    trades-query-id: ${IBKR_FLEX_TRADES_QUERY_ID:}
    base-url: ${IBKR_FLEX_BASE_URL:https://ndcdyn.interactivebrokers.com/AccountManagement/FlexWebService}
    version: ${IBKR_FLEX_VERSION:3}

logging:
  level:
    com.xavopls.ibkr_dashboard: DEBUG
    org.flywaydb: INFO
    org.hibernate.SQL: DEBUG

---
spring:
  config:
    activate:
      on-profile: docker

  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:postgres}:${POSTGRES_PORT:5432}/${POSTGRES_DB:trading_db}
    username: ${POSTGRES_USER:trading_user}
    password: ${POSTGRES_PASSWORD:trading_pass}

logging:
  level:
    com.xavopls.ibkr_dashboard: INFO
    org.hibernate.SQL: WARN
